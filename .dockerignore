venv
.venv
__pycache__
*.py[cod]
*.egg-info
.env
.git
.gitignore
*.md
.pytest_cache
.mypy_cache
.ruff_cache
htmlcov
.coverage

# Never bake credentials into an image layer. `Dockerfile` does `COPY . .`,
# and `server/secrets/firebase-service-account.json` is a live GCP private
# key. CI is safe (checkout never has it) — a local `docker build ./server`
# was not.
secrets/
