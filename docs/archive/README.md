# Archived pre-v2 documentation

> ⚠️ **Historical reference only — do not build against these docs.**
>
> Since 2026-08-05 the single source of truth is `server/docs/` itself —
> `README.md` → `business/` → `architecture/` (+ `architecture/adr/`) → `modules/`.
>
> The files in this folder describe the pre-v2 (v1) system, which remains what is
> deployed to production until v2 replaces it module by module. The previously
> declared canon (the `developer-docs/` Nextra site) is likewise historical.
