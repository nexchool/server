"""Mediums of instruction (English, Gujarati, ...).

The whole HTTP surface is GraphQL: `mediums` to read, `addMedium` /
`updateMedium` / `removeMedium` to change. There is no REST blueprint.
"""

from . import models  # noqa: E402, F401
